[project]
name = "vol2splat"
version = "0.1.0"
description = "Volume to Point Cloud Sampler"
dependencies = [
    "numpy",
    "pyyaml",
    "typer[all]",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project.scripts]
vol2splat = "vol2splat.cli:app"
